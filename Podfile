# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TwitterAuthentication' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterAuthentication
  pod 'R.swift', '6.1.0'                                               # Local String, Image, Fonts, identifiers
  pod 'SDWebImage', '~> 5.0'                                           # Asynchronous image downloader with cache support
end
